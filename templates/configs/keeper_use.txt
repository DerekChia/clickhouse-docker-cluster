<clickhouse>
    <zookeeper>
        {%- for n in range(server_ids | length) %}
        <node index="{{ server_ids[n] }}">
            <host>{{ hostnames[n] }}</host>
            <port>9181</port>
        </node>
        {%- endfor %}
    </zookeeper>
</clickhouse>
